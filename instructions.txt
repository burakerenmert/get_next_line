//a function that reads buffersize, checks if there are \n, using a temp, returns the string if \n exists and then frees the static char. mallocs buffersize + 1 for first step of the read.
//a function that searches for the newline, assigns the line into another string. returns it. mallocs for a propriate size. returns the new stirng that ocntains the line.
//a function that clears our static char, mallocs for a new string for leftovers from the last read, copies those to a new string, frees the static char and returns the new one extracting the leftovers.

utilites
//strchr for checking if there are newlines in the current read
//strlen for malloc to my new string after cleaning the line
//strjoin for merging the current read with temp string


get_next_line
a pointer to return,
a static char to make the reads,
extract the line out of a read,
assign it to the pointer,
free the static char
no malloc needed here.
return NULL if there is no read.
return the pointer in the end.